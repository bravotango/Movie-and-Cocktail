$(document).ready(function () {
  const omdbApiKey = "";
  const theCocktailDbKey = "";
});

function getMovie(movie) {
  console.log(movie);
  // fetch
}

function getCocktail(cocktail) {
  console.log(cocktail);
  // fetch
}

getMovie("The Big Lebowski");
getCocktail("White Russian");
